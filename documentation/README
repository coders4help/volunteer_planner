This folder contains some diagrams created with Dia (http://dia-installer.de/).
Save the diagrams without compression (i.e. as plain XML). Doing so, different versions
can be compared against each other and bulk edits can be done using a text editor.
